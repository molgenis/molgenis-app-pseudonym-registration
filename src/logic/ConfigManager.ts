const INITIAL_STATE = (window as any).__INITIAL_STATE__ || {};

export const pseudonymRegistrationEntity =
  INITIAL_STATE.pseudonymRegistrationEntity;
export const entityPackage = INITIAL_STATE.entityPackage;
export const inputDescription = INITIAL_STATE.inputDescription;
export const resultDescription = INITIAL_STATE.resultDescription;
